{
  "capturedAt": "2026-08-27",
  "capturedFromCommit": "client-1.0 working tree, all workstreams landed",
  "capturedFromBranch": "client-1.0",
  "tolerancePercent": 20,
  "correctness": {
    "description": "Frozen violation counts for the single published package (`client`). The guard fails on any NEW violation beyond these \u2014 it never re-fails on this known, pre-existing debt.",
    "captureMethod": "eslint . -f json (summed errorCount/warningCount across all files); tsc --noEmit over packages/client's two tsconfigs. Re-measured on the client-1.0 branch AFTER every workstream landed, so these are the real figures rather than a mid-carve reading: the repo is one package, the five others were deleted and the old 75-error typecheck debt went with them. Typecheck is a hard zero and eslint carries no errors; the warnings are no-non-null-assertion in test files.",
    "eslint": {
      "errors": 0,
      "warnings": 64
    },
    "typecheck": {
      "totalErrors": 0,
      "byPackage": {
        "client": 0
      }
    }
  },
  "speed": {
    "description": "Gate wall-clock (the CI 'build' job: lint + build + typecheck + test, run sequentially).",
    "gateWallClockSeconds": 185,
    "estimated": true,
    "captureMethod": "This PR is what first adds a `typecheck` step to ci.yml's build job (previously a false-PASS hole: typecheck was soft-gated in agent instructions only, never run in CI). No real CI run of the new job shape exists yet, so this number is estimated: measured local step timings for the OLD shape (eslint 33.3s + pnpm -r build 40.8s + vitest run 57.6s = 131.7s) were compared against the real CI 'build' job duration for commit 8a8f5018 (153s, via `gh api repos/toon-protocol/toon-client/actions/runs/30014129025/jobs`), giving a local->CI overhead ratio of 1.162x. That ratio was applied to the local NEW-shape sum (131.7s + typecheck 27.6s = 159.3s) to project 185s. Refresh with the real measured duration after the first post-merge CI run of this workflow shape; the 20% tolerance is partly there to absorb this estimate's error margin in the meantime."
  },
  "performance": {
    "description": "Gate resource cost, scoped to the same CI 'build' job as speed above (not the separate devbox-validate job, which is an unrelated environment-consistency check).",
    "runnerMinutesEstimate": 3.08,
    "imageSizeBytes": null,
    "cacheHitRatePercent": null,
    "captureMethod": "runnerMinutesEstimate = gateWallClockSeconds / 60 (single ubuntu-latest runner, no parallel jobs within 'build'), carrying the same estimate caveat as speed above. imageSizeBytes and cacheHitRatePercent have no baseline yet: no Docker daemon was available in the capture environment (imageSizeBytes), and no reproducible source for pnpm/actions cache hit-rate was available outside a live CI run (cacheHitRatePercent). Both are null so the guard SKIPS those checks rather than comparing against a fabricated number (no false FAIL) \u2014 see .sandcastle/gate-guard.ts. agent-image.yml now reports image size on its own runs so imageSizeBytes can be backfilled from there."
  }
}
