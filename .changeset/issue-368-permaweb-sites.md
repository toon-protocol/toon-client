---
---

rig: pushed repos as permaweb sites — derive per-file `Content-Type` from the blob path on kind:5094 blob uploads (`packages/rig/src/mime.ts`) and add `rig site publish`/`rig site url` to build and serve an ar.io path manifest per ref. estimate→confirm→execute + strict `--json` + `--yes`. All paid paths are mock-tested only — no funds spent.
