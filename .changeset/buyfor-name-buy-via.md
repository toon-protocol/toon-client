---
'@toon-protocol/rig': minor
---

`rig name buy --via <store-url>` — brokered ArNS purchase through the store's kind:5095 job: spawns a client-owned ANT and submits the buy with its processId, so the store pays mARIO while the client owns the name from inception (toon-meta#162).
