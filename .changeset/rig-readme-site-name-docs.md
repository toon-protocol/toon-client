---
"@toon-protocol/rig": patch
---

docs(rig): document the `rig site` (permaweb sites) and `rig name` (ArNS) verbs in the package README — the push → publish → name → permanent URL flow, plus command-reference rows and the funds/`@ar.io/sdk`/mARIO-not-ILP caveats.
