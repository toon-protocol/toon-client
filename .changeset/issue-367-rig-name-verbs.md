---
---

rig: add the `rig name buy/set/status` ArNS naming verbs (`packages/rig/src/cli/name.ts`), owned and paid by the rig mnemonic's Solana key. estimate→confirm→execute + strict `--json` + `--yes`; `@ar.io/sdk` is an optional, lazily-imported dependency. All money-moving paths are mock-tested only — no real $ARIO spent and no real registry call.
