---
---

CI and test wiring only — no published behaviour changes, so this changeset is
deliberately empty rather than bumping `@toon-protocol/client` for a workflow
and a test file (toon-client#607, toon-client#611).
