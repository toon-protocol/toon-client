---
"@toon-protocol/views": patch
---

Disable the pay-confirm "Confirm & pay" button when the fee status is unavailable (`statusError`), so a user can never click through a "fee unavailable" screen into a silent spend.
