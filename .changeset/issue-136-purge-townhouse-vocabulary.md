---
"@toon-protocol/client-mcp": patch
---

Purge legacy `townhouse` vocabulary: replace `g.townhouse.town` default destination with `g.proxy`, update `g.townhouse.mill`/`g.townhouse.dvm` example addresses to `g.proxy.mill`/`g.proxy.dvm`, and remove all remaining `townhouse` references from source, docs, and tests.
