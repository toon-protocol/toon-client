{
  "name": "@toon-protocol/rig",
  "version": "3.0.0",
  "description": "Git-to-TOON write path core — build git objects and NIP-34 events for the Rig control plane",
  "license": "MIT",
  "author": "Jonathan Green",
  "//": "NOT PUBLISHED FROM THIS REPO. @toon-protocol/rig on npm is owned by github.com/toon-protocol/rig. This copy stays in the workspace because client-mcp bundles it and the local `rig` CLI builds from here; `private: true` is what keeps changesets from publishing it again.",
  "private": true,
  "repository": {
    "type": "git",
    "url": "https://github.com/toon-protocol/toon-client.git",
    "directory": "packages/rig"
  },
  "keywords": [
    "git",
    "nostr",
    "nip-34",
    "toon",
    "arweave",
    "ilp"
  ],
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "bin": {
    "rig": "./dist/cli/rig.js"
  },
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    },
    "./standalone": {
      "types": "./dist/standalone/index.d.ts",
      "import": "./dist/standalone/index.js"
    },
    "./package.json": "./package.json"
  },
  "files": [
    "dist",
    "README.md"
  ],
  "scripts": {
    "build": "tsup",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage"
  },
  "dependencies": {
    "@toon-format/toon": "^1.0.0",
    "@toon-protocol/client": "workspace:^",
    "@toon-protocol/core": "^3.1.2",
    "nostr-tools": "^2.20.0"
  },
  "optionalDependencies": {
    "@ar.io/sdk": "^4.0.3",
    "@ar.io/solana-contracts": "^1.0.1",
    "@solana/kit": "^6.8.0"
  },
  "devDependencies": {
    "@toon-protocol/arweave": "workspace:*",
    "@types/node": "^20.0.0",
    "@types/ws": "^8.18.1",
    "tsup": "^8.0.0",
    "typescript": "^5.3.0",
    "vitest": "^1.0.0",
    "ws": "^8.18.0"
  }
}
