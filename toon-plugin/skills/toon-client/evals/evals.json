{
  "trigger_evals": [
    { "query": "How do I publish a note to TOON?", "should_trigger": true },
    { "query": "How do I post on the TOON network from Claude?", "should_trigger": true },
    { "query": "How do I read events from a TOON relay?", "should_trigger": true },
    { "query": "How do I subscribe to events on TOON?", "should_trigger": true },
    { "query": "Use toon_publish to send this event", "should_trigger": true },
    { "query": "Why is reading free but writing costs money on TOON?", "should_trigger": true },
    { "query": "How do I check my TOON payment channel nonce and balance?", "should_trigger": true },
    { "query": "How do I open a payment channel on TOON?", "should_trigger": true },
    { "query": "Am I connected to the TOON network? What is my TOON address?", "should_trigger": true },
    { "query": "How do I swap tokens via a TOON mill?", "should_trigger": true },
    { "query": "The toon tool says it is bootstrapping, what do I do?", "should_trigger": true },
    { "query": "How does pay-to-write work with payment channel claims on TOON?", "should_trigger": true },
    { "query": "How do I render the latest notes as a feed UI on TOON?", "should_trigger": true },
    { "query": "Use toon_atoms and toon_render to show me a TOON profile", "should_trigger": true },
    { "query": "How do I attach an image to a TOON post?", "should_trigger": true },
    { "query": "Upload a picture to Arweave and post it on TOON with toon_upload", "should_trigger": true },
    { "query": "What is kind:31036 and the render trust gradient on TOON?", "should_trigger": true },
    { "query": "How does a TOON client render an unknown event kind (open-world UI)?", "should_trigger": true },
    { "query": "How do I query TOON events to fill in a rendered view?", "should_trigger": true },
    { "query": "Add another relay and apex to my TOON client", "should_trigger": true },
    { "query": "Fund my devnet TOON wallet so I can pay for writes", "should_trigger": true },
    { "query": "How do I deploy a container to Akash?", "should_trigger": false },
    { "query": "How do I write a unit test in vitest?", "should_trigger": false },
    { "query": "How do I create a badge definition with NIP-58?", "should_trigger": false },
    { "query": "What is the git object binary format?", "should_trigger": false }
  ],
  "output_evals": [
    {
      "id": "publish-note",
      "prompt": "Publish a kind:1 note saying \"gm\" to TOON for me.",
      "expected_output": "Sign the kind:1 event, then call toon_publish with the fully-signed event. Note that the write costs a fee and advances the channel nonce. If toon_status shows bootstrapping, wait and retry. Report the returned eventId and nonce from the tool result; do not invent them.",
      "rubric": {
        "correct": "Uses toon_publish with a fully-signed event; notes the write costs a fee / advances the nonce; handles the bootstrapping-retry case; reads eventId/nonce from the result.",
        "acceptable": "Uses toon_publish with a signed event but omits the fee/bootstrapping nuance.",
        "incorrect": "Tries to publish without signing, fabricates an eventId/nonce, or uses a non-existent tool."
      },
      "assertions": [
        "Response calls toon_publish with a fully-signed Nostr event",
        "Response notes the publish costs a fee and advances the channel nonce",
        "Response handles the bootstrapping/retry case (poll toon_status until ready)",
        "Response reads eventId and nonce from the tool result rather than fabricating them"
      ]
    },
    {
      "id": "read-back",
      "prompt": "Watch for new kind:1 notes from author <hexpubkey> on TOON and show them to me as they arrive.",
      "expected_output": "Call toon_subscribe with filters {authors:[<hexpubkey>], kinds:[1]} to get a subId, then poll toon_read with that subId, passing back the returned cursor each call so only new events are returned. Reads are free.",
      "rubric": {
        "correct": "Uses toon_subscribe then toon_read; passes the cursor back across calls; notes reads are free.",
        "acceptable": "Uses toon_subscribe + toon_read but doesn't mention the cursor long-poll pattern.",
        "incorrect": "Uses a paid tool to read, or invents a reading mechanism not in the toon-* tools."
      },
      "assertions": [
        "Response uses toon_subscribe with a NIP-01 filter, then toon_read",
        "Response passes the returned cursor back on each toon_read to fetch only new events",
        "Response notes that reads are free (no payment)",
        "Response does not use toon_publish or any paid tool to read"
      ]
    },
    {
      "id": "status-bootstrap",
      "prompt": "My toon_publish just returned a message about the client still bootstrapping. What's happening and what should I do?",
      "expected_output": "The daemon's managed anon proxy + BTP session are still coming up (~30-90s on a cold start); the write path is not ready yet. This is not an error. Call toon_status and wait until ready:true, then retry the publish. Free reads may still work meanwhile.",
      "rubric": {
        "correct": "Explains bootstrapping is the anon/BTP warm-up (not an error); says to poll toon_status until ready then retry; notes reads can work meanwhile.",
        "acceptable": "Says to wait and retry but doesn't explain why or mention toon_status.",
        "incorrect": "Treats bootstrapping as a fatal error, or advises re-signing/refunding, or loops the publish blindly."
      },
      "assertions": [
        "Response explains bootstrapping is the anon proxy / BTP warm-up, not an error",
        "Response says to poll toon_status until ready:true before retrying",
        "Response does not blindly retry toon_publish in a tight loop",
        "Response notes free reads can proceed while the write path is bootstrapping"
      ]
    },
    {
      "id": "channel-balance",
      "prompt": "How much have I spent on TOON so far and what's my current channel nonce?",
      "expected_output": "Call toon_channels to list tracked channels with their nonce (watermark) and cumulativeAmount. Report those values from the tool result. Optionally call toon_status / toon_identity for chain + address context. Do not fabricate balances.",
      "rubric": {
        "correct": "Uses toon_channels and reports nonce + cumulativeAmount from the result; does not invent figures.",
        "acceptable": "Uses toon_channels but conflates nonce and amount or omits one.",
        "incorrect": "Fabricates a balance/nonce, or uses a non-existent tool."
      },
      "assertions": [
        "Response uses toon_channels to read nonce and cumulativeAmount",
        "Response reports the values from the tool result rather than inventing them",
        "Response correctly distinguishes nonce (watermark) from cumulativeAmount (spend)",
        "Response treats channel balances as the user's financial state"
      ]
    },
    {
      "id": "render-feed",
      "prompt": "Render the latest kind:1 notes as a feed UI in the host for me.",
      "expected_output": "Call toon_atoms first to learn the atom vocabulary and ViewSpec grammar, then compose a ViewSpec (e.g. a stack with a composer/pay-confirm and a note-card bound via a kindAuto query for kind:1) and pass it to toon_render, which renders the ui://toon/app bundle. Rendering is free; only the writes the atoms fire (post/follow) are paid.",
      "rubric": {
        "correct": "Calls toon_atoms then toon_render with a valid ViewSpec using catalog atoms (e.g. note-card with a kind:1 bind); notes rendering is free while atom writes are paid.",
        "acceptable": "Uses toon_render with a ViewSpec but skips toon_atoms or omits the free-render/paid-write distinction.",
        "incorrect": "Invents a rendering tool, hand-writes HTML, uses an atom not in the catalog, or treats rendering itself as a paid write."
      },
      "assertions": [
        "Response calls toon_atoms to learn the atom vocabulary before composing a view",
        "Response calls toon_render with a ViewSpec of catalog atoms (e.g. note-card bound to a kind:1 query)",
        "Response notes the view renders as ui://toon/app in the host",
        "Response notes rendering is free while the writes its atoms fire are paid"
      ]
    },
    {
      "id": "upload-image",
      "prompt": "Attach this image to a post on TOON.",
      "expected_output": "Use toon_upload_media with the base64 image bytes (and mime); it uploads to Arweave via the kind:5094 DVM and then publishes a media event (default kind 1063, or 20 for a picture / 1 with NIP-92 imeta) referencing the Arweave URL. This is a paid two-step write that advances the channel nonce. Report the returned eventId/URL from the result.",
      "rubric": {
        "correct": "Uses toon_upload_media with base64 bytes; notes it uploads to Arweave then publishes a media event and that it is a paid write; reads eventId/URL from the result.",
        "acceptable": "Uses toon_upload_media but omits the Arweave/two-step or paid nuance.",
        "incorrect": "Uses toon_publish with a fabricated image URL, invents an upload mechanism, or ignores that it costs a fee."
      },
      "assertions": [
        "Response uses toon_upload_media (not a fabricated URL via toon_publish)",
        "Response notes it uploads bytes to Arweave then publishes a media event",
        "Response notes the upload is a paid write that advances the channel nonce",
        "Response reads the resulting eventId / Arweave URL from the tool result"
      ]
    },
    {
      "id": "render-gradient",
      "prompt": "What is kind:31036 and how does the TOON render trust gradient handle an unknown event kind?",
      "expected_output": "Explain that a kind is an open component-catalog key, so the client forks on 'do I know this kind?': known kind -> native atom (full trust); unknown kind -> resolve the event's ui tag to an addressable kind:31036 renderer authored by the event author (d = target kind), re-verify its signature, and select a branch by the renderer's m (mimeType) tag: application/a2ui+json -> branch 2 (A2UI, medium), text/html;profile=mcp-app -> branch 3 (sandboxed mcp-ui, low); no renderer -> branch 4 generative fallback (low). A swap-defense guard fails closed; a client can publish kind:31036 renderers (including generated ones) back.",
      "rubric": {
        "correct": "Describes the four branches keyed on known-kind; explains kind:31036 as an addressable, author-bound renderer selected by its m tag; mentions signature re-verify / fail-closed guard and that renderers can be published back.",
        "acceptable": "Describes the gradient and kind:31036 but misses the m-tag branch selector or the author-binding/verification.",
        "incorrect": "Invents a different rendering mechanism, claims unknown kinds error out, or says any provider can publish a renderer for someone else's event."
      },
      "assertions": [
        "Response describes the four-branch gradient keyed on whether the client knows the kind",
        "Response explains kind:31036 as an addressable renderer (d = target kind) authored by the event author",
        "Response says the renderer's m (mimeType) tag selects the branch (a2ui+json -> A2UI, text/html;profile=mcp-app -> sandboxed mcp-ui)",
        "Response notes the client re-verifies the renderer signature / fails closed and can publish kind:31036 renderers back"
      ]
    }
  ]
}
